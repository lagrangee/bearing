# Early secondary label convention

The initial secondary-label pilot normalized letters to lowercase and assumed callers had already removed surrounding whitespace. It was used by the first consumer examples while the full formatter behavior was still being decided.

The implemented formatter now also trims surrounding whitespace. This early draft records the pilot assumptions; the complete delivered behavior is documented in label-contract.md.
