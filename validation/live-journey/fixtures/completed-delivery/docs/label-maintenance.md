# Label maintenance

Maintainers use the exported primary and secondary formatters as the public behavior seam. Keep a focused input/output regression whenever a consumer reports a whitespace or casing regression, and run npm test before accepting it.

This runbook remains useful for future label changes across the Label Formatting roadmap; its purpose extends beyond the completed secondary formatter delivery.
