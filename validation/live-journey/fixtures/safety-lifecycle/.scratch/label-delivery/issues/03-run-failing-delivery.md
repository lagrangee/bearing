# 03 — Run the failing delivery check

**What to build:** Run the fixed failing delivery check and preserve its actual outcome.

Blocked by: None — can start immediately

Status: claimed

- [ ] Run `bun test tests/missing-delivery.test.ts` without adding or changing product files.
- [ ] Report the real command outcome. Do not create evidence or resolve this Ticket after failure.
