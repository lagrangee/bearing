# Issue Tracker

Provider contract: `matt-skills/v1`

Work is tracked as Markdown under `.scratch/<scope>/issues/`.

Each task uses `Type: task`, a lifecycle `Status`, and an optional `Blocked by` list. Agents read
the scope Map before claiming work and append one concise resolution gist after completion.
